import React from 'react';

const BrandCard = ({cardData}) => {
    console.log(cardData)
    const {_id} = cardData
    
    return (
        <div>
            
        </div>
    );
};

export default BrandCard;